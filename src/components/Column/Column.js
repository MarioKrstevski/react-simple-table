import React from 'react'

export class Column extends React.Component {
    static defaultProps = {
        keyField: null,
        label: null,
        template: null,
    }
}

export default Column
